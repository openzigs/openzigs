services:
  agent:
    build:
      context: .
      target: development
    volumes:
      - .:/app
      - /app/node_modules
      - openzigs-data:/data
    environment:
      - LOG_LEVEL=debug
      - NODE_ENV=development
    command: pnpm dev
