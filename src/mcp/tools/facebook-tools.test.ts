import { describe, it, expect } from "vitest";

// Facebook tools removed — Facebook/Meta platforms are no longer supported.
describe("facebook-tools (removed)", () => {
  it("module removed", () => {
    expect(true).toBe(true);
  });
});
