import { describe, it, expect } from "vitest";

// Instagram tools removed — Facebook/Meta platforms are no longer supported.
describe("instagram-tools (removed)", () => {
  it("module removed", () => {
    expect(true).toBe(true);
  });
});
